#include<stdio.h>
/**
*main - Entry point
*
*Return: Always 0 (Success)
*/
int main(void)
{
for (char digit = '0'; digit <= '9'; digit++)
{
putchar(digit);
}

for (char letter = 'a'; letter <= 'f'; letter++)
{
utchar(letter);
}

putchar('\n');

return (0);
}
